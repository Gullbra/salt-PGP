export const hey = 5

/*
export function debounce (cb:any, delay = 1000) {
  let timeout

  return (...args) => {
    clearTimeout(timeout)
    timeout = setTimeout(()=> => {
      cb(...args)
    }, delay)
  }
}
*/